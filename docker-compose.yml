services:
  web:
    build: .
    ports:
      - "8765:8765"
    env_file:
      - .env
    environment:
      APP_ENV: production
      HOST: 0.0.0.0
      PORT: 8765
    volumes:
      - ppm-data:/root/.passport-photo-maker
    restart: unless-stopped

volumes:
  ppm-data:
